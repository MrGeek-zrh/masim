#regions
# name, length（200MB）
a, 200000000

# phase 1
# name of phase
huge phase 1
# time in ms
5000
# access patterns
# name of region, randomness(0,顺序访问；1，随机访问), stride（步长，字节为单位）, probability
a, 1, 64, 80
